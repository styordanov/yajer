{
	"name": "yajer",
	"displayName": "YAJER",
	"description": "Yet Another Jest Runner",
	"publisher": "styordanov",
	"repository": {
		"type": "git",
		"url": "https://github.com/styordanov/yajer.git"
	},
	"version": "0.1.0",
	"engines": {
		"vscode": "^1.39.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"*"
	],
	"contributes": {
		"views": {
			"explorer": [
				{
					"id": "yajer",
					"name": "YAJER"
				}
			]
		},
		"commands": [
			{
				"command": "yajer.run.test.last.config",
				"title": "%command.run.test.last.config%",
				"icon": {
					"dark": "/media/run-dark.svg",
					"light": "/media/run-light.svg"
				}
			},
			{
				"command": "yajer.run.test.force.config",
				"title": "%command.run.test.force.config%"
			},
			{
				"command": "yajer.run.file.last.config",
				"title": "%command.run.file.last.config%"
			},
			{
				"command": "yajer.run.file.force.config",
				"title": "%command.run.file.force.config%"
			},
			{
				"command": "yajer.debug.test.last.config",
				"title": "%command.debug.test.last.config%"
			},
			{
				"command": "yajer.debug.test.force.config",
				"title": "%command.debug.test.force.config%"
			}
		],
		"menus": {
			"view/item/context": [
				{
					"command": "yajer.run.test.last.config",
					"when": "view == yajer && viewItem == test",
					"group": "inline"
				},
				{
					"command": "yajer.run.test.last.config",
					"when": "view == yajer && viewItem == test",
					"group": "navigation@1"
				},
				{
					"command": "yajer.run.test.force.config",
					"when": "view == yajer && viewItem == test",
					"group": "navigation@2"
				},
				{
					"command": "yajer.run.file.last.config",
					"when": "view == yajer && viewItem == test",
					"group": "navigation@3"
				},
				{
					"command": "yajer.run.file.force.config",
					"when": "view == yajer && viewItem == test",
					"group": "navigation@4"
				},
				{
					"command": "yajer.debug.test.last.config",
					"when": "view == yajer && viewItem == test",
					"group": "navigation@5"
				},
				{
					"command": "yajer.debug.test.force.config",
					"when": "view == yajer && viewItem == test",
					"group": "navigation@6"
				}
			]
		}
	},
	"main": "./out/extension.js",
	"scripts": {
		"vscode:prepublish": "yarn run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"pretest": "yarn run compile",
		"test": "node ./out/test/runTest.js"
	},
	"devDependencies": {
		"@types/glob": "^7.1.1",
		"@types/mocha": "^5.2.6",
		"@types/node": "^10.12.21",
		"@types/vscode": "^1.39.0",
		"glob": "^7.1.4",
		"mocha": "^6.1.4",
		"tslint": "^5.12.1",
		"typescript": "^3.3.1",
		"vscode-test": "^1.2.0"
	},
	"dependencies": {
		"upath": "^1.2.0"
	}
}
