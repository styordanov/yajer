import RunTestCommand from './run-test-command';
import RunFileCommand from './run-file-command';

export const runTestCommand = new RunTestCommand();
export const runFileCommand = new RunFileCommand();
